import styles from "./AboutMe.module.css"

export default function AboutMe() {
    return (
      <div className={styles.AboutMe}>
        <h1 className={styles.header}>AboutMe</h1>
      </div>
    );
  }